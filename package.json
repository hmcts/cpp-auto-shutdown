{
  "name": "cpp-auto-shutdown-dashboard",
  "version": "1.0.0",
  "description": "Environment Board — static dashboard for CRIME non-production stacks",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8000 --directory docs"
  },
  "license": "MIT"
}
